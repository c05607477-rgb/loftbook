# LoftBook — Vite + React

A racing pigeon LoftBook app with ring scanning, automatic IHU prefix formatting, and offline-ready behaviour.

## 🚀 Run locally
1. Make sure you have Node.js installed (v18+)
2. Run:

```bash
npm install
npm run dev
